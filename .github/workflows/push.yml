name: push-workflow

on:
  push:
    branches:
      - "main"

jobs:
  workflow-job:
    name: Run on Push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
              
      - name: Run Maven
        run: mvn clean package
        shell: bash